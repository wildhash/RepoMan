services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - repoman_knowledge:/app/repoman_knowledge

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - api

volumes:
  repoman_knowledge:
